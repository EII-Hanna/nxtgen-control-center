-- Fireflies integration intentionally disabled.
-- Meeting capture will be implemented through Zoom/calendar providers in a separate,
-- provider-neutral migration. This no-op file preserves the migration version so
-- existing Supabase migration history remains consistent.

select 1;
